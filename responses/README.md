# agentic-stacks

`agentic-stacks` is a FastAPI gateway that exposes an OpenAI-style **Responses API** (`/v1/responses`)
in front of an OpenAI-compatible upstream (e.g. **vLLM** `/v1/chat/completions`).

Repository (docs, design docs, examples) lives at the project root.

